{
  "name": "slothfulchat-web",
  "private": true,
  "version": "0.0.0",
  "description": "Feasibility prototype: DeltaChat fully in the browser (WASM core + PWA). Not affiliated with Delta Chat.",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "api-docs": "cd build/core/deltachat-jsonrpc/typescript && ./node_modules/.bin/typedoc --options ../../../../packages/web-app/api-docs/typedoc.json",
    "apply-patches": "scripts/apply-patches.sh",
    "update-patches": "scripts/update-patches.sh",
    "mark-fork-api": "node scripts/mark-fork-api.mjs",
    "verify-fork-marks": "node scripts/verify-fork-marks.mjs",
    "set-version": "node scripts/set-release-version.mjs",
    "smoke": "node scripts/smoke-core-wasm.mjs"
  },
  "packageManager": "pnpm@11.10.0",
  "devDependencies": {
    "playwright": "^1.61.1",
    "ws": "^8.21.0"
  }
}
