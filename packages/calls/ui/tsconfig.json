{
  // ui/ is React: ring/incoming dialog, in-call window, device pickers, etc.
  // (docs/calls.md). Free to import engine/ and bridge/.
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "jsx": "react-jsx"
  },
  "include": ["."]
}
