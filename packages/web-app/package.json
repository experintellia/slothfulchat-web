{
  "name": "@slothfulchat/web-app",
  "version": "0.8.0",
  "private": true,
  "type": "module",
  "description": "deltachat-desktop's frontend running standalone in the browser on the wasm core.",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "assemble": "node assemble.mjs",
    "build": "tsc --noEmit && esbuild --format=esm --bundle src/runtime.ts --outfile=dist/runtime.js && esbuild --format=esm --bundle src/call-popup.ts --outfile=dist/call-popup.js && esbuild --format=esm --bundle src/html-email.ts --outfile=dist/html-email.js && esbuild --format=esm --bundle src/blobs-sw.ts --outfile=dist/blobs-sw.js && node sw-manifest.mjs",
    "dev:webxdc": "node dev-caddy.mjs",
    "serve": "node serve.mjs",
    "start": "pnpm assemble && pnpm build && pnpm serve",
    "test": "node ../../scripts/smoke-web-app.mjs"
  },
  "dependencies": {
    "@slothfulchat/calls": "workspace:*",
    "@slothfulchat/core-wasm": "workspace:*",
    "dompurify": "^3.2.6"
  },
  "devDependencies": {
    "@emotion/react": "^11.14.0",
    "@emotion/styled": "^11.14.1",
    "@mui/icons-material": "6.3.1",
    "@mui/lab": "6.0.0-beta.22",
    "@mui/material": "6.3.1",
    "@open-rpc/docs-react": "2.1.1",
    "esbuild": "^0.25.5",
    "fflate": "^0.8.2",
    "react": "18.3.1",
    "react-dom": "18.3.1",
    "sass": "^1.101.0",
    "typescript": "^5.8.0"
  }
}
