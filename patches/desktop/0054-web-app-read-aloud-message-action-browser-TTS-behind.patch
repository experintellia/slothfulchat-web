From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: experintellia <280144245+experintellia@users.noreply.github.com>
Date: Mon, 20 Jul 2026 01:19:55 +0000
Subject: [PATCH] web-app: read-aloud message action (browser TTS) behind an
 experimental setting

A "Read aloud" item in the message context menu speaks the message text
with the browser's built-in speechSynthesis -- fully local (OS voices),
no dependency, no network. While speech is playing the menu also offers
"Stop reading aloud". Closes #111 (slothfulchat-web).

Gated behind the new off-by-default `experimentalReadAloud` desktop
setting, mirroring `experimentalCompletionMenu` (patch 0050): one key in
DesktopSettingsType + getDefaultState(), one DesktopSettingsSwitch under
Settings -> Advanced -> Experimental features. The menu items are built
per right-click, so the speechSynthesis.speaking check for the stop item
is evaluated fresh each time. Utterances use the browser's default
voice/language; there is no API to detect a message's language.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
---
 _locales/_untranslated_en.json                |  6 +++++
 .../Settings/ExperimentalFeatures.tsx         |  5 ++++
 .../src/components/message/Message.tsx        | 25 +++++++++++++++++++
 packages/shared/shared-types.d.ts             |  2 ++
 packages/shared/state.ts                      |  1 +
 5 files changed, 39 insertions(+)

diff --git a/_locales/_untranslated_en.json b/_locales/_untranslated_en.json
index fe3dfc4b3..5a2b9f2b1 100644
--- a/_locales/_untranslated_en.json
+++ b/_locales/_untranslated_en.json
@@ -112,5 +112,11 @@
   },
   "webxdc_store_url_explain_2_desktop": {
     "message": "This is different from the same feature on Android and iOS.\nThis must be the path containing the xdcget-lock.json file."
+  },
+  "menu_read_aloud": {
+    "message": "Read aloud"
+  },
+  "menu_stop_reading_aloud": {
+    "message": "Stop reading aloud"
   }
 }
diff --git a/packages/frontend/src/components/Settings/ExperimentalFeatures.tsx b/packages/frontend/src/components/Settings/ExperimentalFeatures.tsx
index f9af952db..2a0b05acd 100644
--- a/packages/frontend/src/components/Settings/ExperimentalFeatures.tsx
+++ b/packages/frontend/src/components/Settings/ExperimentalFeatures.tsx
@@ -86,6 +86,11 @@ export function ExperimentalFeatures() {
         label='Estimated read time on unread badge'
         description='Show roughly how long the unread messages in a chat take to read (e.g. "~4 min") next to the unread counter. Off by default (experimental).'
       />
+      <DesktopSettingsSwitch
+        settingsKey='experimentalReadAloud'
+        label='Read messages aloud'
+        description="Adds a 'Read aloud' action to the message menu that speaks the message with your device's built-in voices. Fully local, nothing leaves your device; voice quality depends on your OS and browser. Off by default (experimental)."
+      />
       {runtime.getRuntimeInfo().isContentProtectionSupported && (
         <DesktopSettingsSwitch
           settingsKey='contentProtectionEnabled'
diff --git a/packages/frontend/src/components/message/Message.tsx b/packages/frontend/src/components/message/Message.tsx
index 65171d5ea..57e44dbd6 100644
--- a/packages/frontend/src/components/message/Message.tsx
+++ b/packages/frontend/src/components/message/Message.tsx
@@ -33,6 +33,7 @@ import { mapCoreMsgStatus2String } from '../helpers/MapMsgStatus'
 import { ContextMenuItem } from '../ContextMenu'
 import { onDCEvent, BackendRemote } from '../../backend-com'
 import { selectedAccountId } from '../../ScreenController'
+import SettingsStoreInstance from '../../stores/settings'
 import { ProtectionEnabledDialog } from '../dialogs/ProtectionStatusDialog'
 import useDialog from '../../hooks/dialog/useDialog'
 import useMessage from '../../hooks/chat/useMessage'
@@ -312,6 +313,14 @@ function buildContextMenu(
   // Do not show "react" for system messages
   const showSendReaction = showReactionsUi(message, chat)
 
+  // "Read aloud" via the browser's built-in speechSynthesis (experimental,
+  // off by default). Fully local: OS voices, no network.
+  const showReadAloud =
+    SettingsStoreInstance.state?.desktopSettings.experimentalReadAloud ===
+      true &&
+    'speechSynthesis' in window &&
+    !!text
+
   // Only show in groups, don't show on info messages or outgoing messages
   const showReplyPrivately =
     (conversationType.chatType === 'Group' ||
@@ -387,6 +396,22 @@ function buildContextMenu(
       },
     // copy item (selection or all text)
     text !== '' && copy_item,
+    // Read message aloud (experimental)
+    showReadAloud && {
+      label: tx('menu_read_aloud'),
+      action: () => {
+        // ponytail: default voice/language; the browser picks per its locale,
+        // a per-message language guess would need detection with no API for it
+        window.speechSynthesis.cancel()
+        window.speechSynthesis.speak(new SpeechSynthesisUtterance(text))
+      },
+    },
+    // Stop an ongoing read-aloud (only shown while speaking)
+    showReadAloud &&
+      window.speechSynthesis.speaking && {
+        label: tx('menu_stop_reading_aloud'),
+        action: () => window.speechSynthesis.cancel(),
+      },
     // Copy image
     showCopyImage && {
       label: tx('menu_copy_image_to_clipboard'),
diff --git a/packages/shared/shared-types.d.ts b/packages/shared/shared-types.d.ts
index 624d5b1ba..e60248b21 100644
--- a/packages/shared/shared-types.d.ts
+++ b/packages/shared/shared-types.d.ts
@@ -45,6 +45,8 @@ export interface DesktopSettingsType {
   experimentalCompletionMenu: boolean
   /** webapp: estimated time-to-read next to the unread badge. Off by default (experimental). */
   experimentalUnreadReadTime: boolean
+  /** webapp: "Read aloud" message action via browser speechSynthesis. Off by default (experimental). */
+  experimentalReadAloud: boolean
   enterKeySends: boolean
   locale: string | null
   notifications: boolean
diff --git a/packages/shared/state.ts b/packages/shared/state.ts
index fb007364d..69bf299de 100644
--- a/packages/shared/state.ts
+++ b/packages/shared/state.ts
@@ -17,6 +17,7 @@ export function getDefaultState(): DesktopSettingsType {
     linkPreviewSuggestions: false,
     experimentalCompletionMenu: false,
     experimentalUnreadReadTime: false,
+    experimentalReadAloud: false,
     chatViewBgImg: undefined,
     lastChats: {},
     zoomFactor: undefined,
