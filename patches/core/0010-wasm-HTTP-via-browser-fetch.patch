From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: experintellia <280144245+experintellia@users.noreply.github.com>
Date: Tue, 7 Jul 2026 22:52:15 +0000
Subject: [PATCH 10/30] wasm: HTTP via browser fetch()

Replace the http_wasm.rs stubs with a fetch()-backed implementation.
The browser owns TLS/redirects/caching; requests are subject to CORS
(apps.testrun.org sends ACAO:*, so the webxdc apps list works).
JS futures are !Send, so the fetch runs in a spawn_local task and
reports back over a oneshot channel, like ws_tcp.
---
 Cargo.lock                                  |  166 +-
 Cargo.toml                                  |    2 +
 src/net/http_wasm.rs                        |  156 +-
 3 files changed, 301 insertions(+), 23 deletions(-)

diff --git a/Cargo.lock b/Cargo.lock
index a5493ca..b840b94 100644
--- a/Cargo.lock
+++ b/Cargo.lock
@@ -2,6 +2,18 @@
 # It is not intended for manual editing.
 version = 4
 
+[[package]]
+name = "accessory"
+version = "2.1.0"
+source = "registry+https://github.com/rust-lang/crates.io-index"
+checksum = "28e416a3ab45838bac2ab2d81b1088d738d7b2d2c5272a54d39366565a29bd80"
+dependencies = [
+ "macroific",
+ "proc-macro2",
+ "quote",
+ "syn 2.0.118",
+]
+
 [[package]]
 name = "addr2line"
 version = "0.25.1"
@@ -1347,6 +1359,20 @@ dependencies = [
  "generic-array",
 ]
 
+[[package]]
+name = "delegate-display"
+version = "3.0.0"
+source = "registry+https://github.com/rust-lang/crates.io-index"
+checksum = "9926686c832494164c33a36bf65118f4bd6e704000b58c94681bf62e9ad67a74"
+dependencies = [
+ "impartial-ord",
+ "itoa",
+ "macroific",
+ "proc-macro2",
+ "quote",
+ "syn 2.0.118",
+]
+
 [[package]]
 name = "deltachat"
 version = "2.54.0-dev"
@@ -1436,7 +1462,9 @@ dependencies = [
  "url",
  "uuid",
  "walkdir",
+ "wasm-bindgen",
  "wasm-bindgen-futures",
+ "web-sys",
  "webpki-roots",
  "ws_stream_wasm",
 ]
@@ -2028,6 +2056,18 @@ version = "0.1.9"
 source = "registry+https://github.com/rust-lang/crates.io-index"
 checksum = "7360491ce676a36bf9bb3c56c1aa791658183a54d2744120f27285738d90465a"
 
+[[package]]
+name = "fancy_constructor"
+version = "2.1.0"
+source = "registry+https://github.com/rust-lang/crates.io-index"
+checksum = "28a27643a5d05f3a22f5afd6e0d0e6e354f92d37907006f97b84b9cb79082198"
+dependencies = [
+ "macroific",
+ "proc-macro2",
+ "quote",
+ "syn 2.0.118",
+]
+
 [[package]]
 name = "fast-socks5"
 version = "1.0.0"
@@ -2993,6 +3033,51 @@ dependencies = [
  "nom 7.1.3",
 ]
 
+[[package]]
+name = "impartial-ord"
+version = "1.0.6"
+source = "registry+https://github.com/rust-lang/crates.io-index"
+checksum = "0ab604ee7085efba6efc65e4ebca0e9533e3aff6cb501d7d77b211e3a781c6d5"
+dependencies = [
+ "proc-macro2",
+ "quote",
+ "syn 2.0.118",
+]
+
+[[package]]
+name = "indexed_db_futures"
+version = "0.6.4"
+source = "registry+https://github.com/rust-lang/crates.io-index"
+checksum = "69ff41758cbd104e91033bb53bc449bec7eea65652960c81eddf3fc146ecea19"
+dependencies = [
+ "accessory",
+ "cfg-if",
+ "delegate-display",
+ "derive_more 2.0.1",
+ "fancy_constructor",
+ "indexed_db_futures_macros_internal",
+ "js-sys",
+ "sealed",
+ "smallvec",
+ "thiserror 2.0.18",
+ "tokio",
+ "wasm-bindgen",
+ "wasm-bindgen-futures",
+ "web-sys",
+]
+
+[[package]]
+name = "indexed_db_futures_macros_internal"
+version = "1.0.0"
+source = "registry+https://github.com/rust-lang/crates.io-index"
+checksum = "caeba94923b68f254abef921cea7e7698bf4675fdd89d7c58bf1ed885b49a27d"
+dependencies = [
+ "macroific",
+ "proc-macro2",
+ "quote",
+ "syn 2.0.118",
+]
+
 [[package]]
 name = "indexmap"
 version = "2.11.4"
@@ -3000,7 +3085,7 @@ source = "registry+https://github.com/rust-lang/crates.io-index"
 checksum = "4b0f83760fb341a774ed326568e19f5a863af4a952def8c39f9ab92fd95b88e5"
 dependencies = [
  "equivalent",
- "hashbrown 0.15.4",
+ "hashbrown 0.16.1",
 ]
 
 [[package]]
@@ -3499,6 +3584,54 @@ version = "0.11.11"
 source = "registry+https://github.com/rust-lang/crates.io-index"
 checksum = "9106e1d747ffd48e6be5bb2d97fa706ed25b144fbee4d5c02eae110cd8d6badd"
 
+[[package]]
+name = "macroific"
+version = "2.0.0"
+source = "registry+https://github.com/rust-lang/crates.io-index"
+checksum = "89f276537b4b8f981bf1c13d79470980f71134b7bdcc5e6e911e910e556b0285"
+dependencies = [
+ "macroific_attr_parse",
+ "macroific_core",
+ "macroific_macro",
+]
+
+[[package]]
+name = "macroific_attr_parse"
+version = "2.0.0"
+source = "registry+https://github.com/rust-lang/crates.io-index"
+checksum = "ad4023761b45fcd36abed8fb7ae6a80456b0a38102d55e89a57d9a594a236be9"
+dependencies = [
+ "proc-macro2",
+ "quote",
+ "sealed",
+ "syn 2.0.118",
+]
+
+[[package]]
+name = "macroific_core"
+version = "2.0.0"
+source = "registry+https://github.com/rust-lang/crates.io-index"
+checksum = "d0a7594d3c14916fa55bef7e9d18c5daa9ed410dd37504251e4b75bbdeec33e3"
+dependencies = [
+ "proc-macro2",
+ "quote",
+ "sealed",
+ "syn 2.0.118",
+]
+
+[[package]]
+name = "macroific_macro"
+version = "2.0.0"
+source = "registry+https://github.com/rust-lang/crates.io-index"
+checksum = "4da6f2ed796261b0a74e2b52b42c693bb6dee1effba3a482c49592659f824b3b"
+dependencies = [
+ "macroific_attr_parse",
+ "macroific_core",
+ "proc-macro2",
+ "quote",
+ "syn 2.0.118",
+]
+
 [[package]]
 name = "mail-builder"
 version = "0.4.4"
@@ -5496,6 +5629,17 @@ dependencies = [
  "url",
 ]
 
+[[package]]
+name = "sealed"
+version = "0.6.0"
+source = "registry+https://github.com/rust-lang/crates.io-index"
+checksum = "22f968c5ea23d555e670b449c1c5e7b2fc399fdaec1d304a17cd48e288abc107"
+dependencies = [
+ "proc-macro2",
+ "quote",
+ "syn 2.0.118",
+]
+
 [[package]]
 name = "sec1"
 version = "0.7.3"
@@ -5978,6 +6122,22 @@ dependencies = [
  "wasm-bindgen",
 ]
 
+[[package]]
+name = "sqlite-wasm-vfs"
+version = "0.2.0"
+source = "registry+https://github.com/rust-lang/crates.io-index"
+checksum = "7f7a5c9ac229421d577bb5a9bb59048838509958b218dd4e0b3c1214a87c361e"
+dependencies = [
+ "indexed_db_futures",
+ "js-sys",
+ "rsqlite-vfs",
+ "thiserror 2.0.18",
+ "tokio",
+ "wasm-bindgen",
+ "wasm-bindgen-futures",
+ "web-sys",
+]
+
 [[package]]
 name = "stable_deref_trait"
 version = "1.2.0"
@@ -6460,9 +6620,13 @@ version = "1.0.0"
 dependencies = [
  "futures",
  "js-sys",
+ "sqlite-wasm-rs",
+ "sqlite-wasm-vfs",
  "tokio",
+ "wasm-bindgen",
  "wasm-bindgen-futures",
  "wasmtimer",
+ "web-sys",
 ]
 
 [[package]]
diff --git a/Cargo.toml b/Cargo.toml
index 489250a..adb8ad9 100644
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -135,7 +135,9 @@ js-sys = "0.3"
 pgp = { version = "0.20.0", default-features = false, features = ["wasm"] }
 tokio-util = { workspace = true, features = ["compat"] }
 uuid = { version = "1", features = ["js"] }
+wasm-bindgen = "0.2"
 wasm-bindgen-futures = "0.4"
+web-sys = { version = "0.3", features = ["Headers", "RequestInit", "Response", "WorkerGlobalScope"] }
 ws_stream_wasm = "0.7"
 
 [dev-dependencies]
diff --git a/src/net/http_wasm.rs b/src/net/http_wasm.rs
index 2319619..24520a0 100644
--- a/src/net/http_wasm.rs
+++ b/src/net/http_wasm.rs
@@ -1,11 +1,15 @@
-//! wasm32 stub for the HTTP module.
+//! wasm32 HTTP module backed by the browser `fetch()` API.
 //!
-//! The native implementation uses hyper over raw TCP. On wasm every request
-//! errors at runtime; routing HTTP through `fetch()` or the WebSocket proxy
-//! is the upgrade path (M3+).
+//! The native implementation uses hyper over raw TCP. Here the browser owns
+//! the connection: TLS, redirects and caching are handled by `fetch()`, and
+//! requests are subject to CORS.
 
-use anyhow::{Result, bail};
+use anyhow::{Context as _, Result, anyhow, bail};
+use futures::channel::oneshot;
+use mime::Mime;
 use serde::Serialize;
+use wasm_bindgen::{JsCast, JsValue};
+use wasm_bindgen_futures::JsFuture;
 
 use crate::context::Context;
 
@@ -22,54 +26,162 @@ pub struct Response {
     pub encoding: Option<String>,
 }
 
+/// The parts of a `fetch()` response we use; unlike JS handles, all Send.
+struct FetchResponse {
+    status: u16,
+    ok: bool,
+    content_type: Option<String>,
+    body: Vec<u8>,
+}
+
+fn js_str(v: JsValue) -> String {
+    format!("{v:?}")
+}
+
+/// Runs `fetch()` on the worker's JS event loop.
+///
+/// JS handles are `!Send` but our futures must be Send (yerpc), so the JS
+/// side runs in a `spawn_local` task and the result comes back over a
+/// oneshot channel — same pattern as `ws_tcp`.
+async fn fetch(
+    method: &'static str,
+    url: &str,
+    body: Option<String>,
+    content_type: Option<&'static str>,
+) -> Result<FetchResponse> {
+    let url = url.to_string();
+    let (tx, rx) = oneshot::channel();
+    wasm_bindgen_futures::spawn_local(async move {
+        let _ = tx.send(fetch_inner(method, &url, body, content_type).await);
+    });
+    rx.await
+        .map_err(|_| anyhow!("fetch task dropped"))?
+        .map_err(|e| anyhow!("fetch {method} {e}"))
+}
+
+async fn fetch_inner(
+    method: &str,
+    url: &str,
+    body: Option<String>,
+    content_type: Option<&str>,
+) -> std::result::Result<FetchResponse, String> {
+    let init = web_sys::RequestInit::new();
+    init.set_method(method);
+    if let Some(body) = &body {
+        init.set_body(&JsValue::from_str(body));
+    }
+    if let Some(ct) = content_type {
+        let headers = web_sys::Headers::new().map_err(js_str)?;
+        headers.append("content-type", ct).map_err(js_str)?;
+        init.set_headers(&headers);
+    }
+    // The core always runs in a Web Worker; there is no `window` global.
+    let scope: web_sys::WorkerGlobalScope = js_sys::global().unchecked_into();
+    let resp: web_sys::Response = JsFuture::from(scope.fetch_with_str_and_init(url, &init))
+        .await
+        .map_err(js_str)?
+        .unchecked_into();
+    let buf = JsFuture::from(resp.array_buffer().map_err(js_str)?)
+        .await
+        .map_err(js_str)?;
+    Ok(FetchResponse {
+        status: resp.status(),
+        ok: resp.ok(),
+        content_type: resp.headers().get("content-type").ok().flatten(),
+        body: js_sys::Uint8Array::new(&buf).to_vec(),
+    })
+}
+
+/// POST endpoints carry tokens; https only, as on native.
+fn ensure_https(url: &str) -> Result<()> {
+    if !url.starts_with("https://") {
+        bail!("POST requests to non-HTTPS URLs are not allowed");
+    }
+    Ok(())
+}
+
 /// Retrieves the text contents of URL using HTTP GET request.
-pub async fn read_url(_context: &Context, _url: &str) -> Result<String> {
-    bail!("HTTP requests are not available on wasm yet")
+pub async fn read_url(context: &Context, url: &str) -> Result<String> {
+    let response = read_url_blob(context, url).await?;
+    Ok(String::from_utf8_lossy(&response.blob).to_string())
 }
 
 /// Retrieves the text contents of URL using HTTP GET request.
 pub(crate) async fn read_url_with_tls(
-    _context: &Context,
-    _url: &str,
+    context: &Context,
+    url: &str,
     _strict_tls: bool,
 ) -> Result<String> {
-    bail!("HTTP requests are not available on wasm yet")
+    // The browser owns TLS on the fetch path; `strict_tls` has no meaning here.
+    read_url(context, url).await
 }
 
 /// Removes expired cache entries.
 pub(crate) async fn http_cache_cleanup(_context: &Context) -> Result<()> {
+    // No core-side HTTP cache on wasm; the browser HTTP cache covers GETs.
     Ok(())
 }
 
 /// Retrieves the binary contents of URL using HTTP GET request.
-pub async fn read_url_blob(_context: &Context, _url: &str) -> Result<Response> {
-    bail!("HTTP requests are not available on wasm yet")
+pub async fn read_url_blob(_context: &Context, url: &str) -> Result<Response> {
+    let resp = fetch("GET", url, None, None).await?;
+    if !resp.ok {
+        bail!(
+            "The server returned a non-successful response code: {}",
+            resp.status
+        );
+    }
+    let content_type = resp.content_type.and_then(|v| v.parse::<Mime>().ok());
+    let mimetype = content_type.as_ref().map(|m| m.essence_str().to_string());
+    let encoding = content_type
+        .as_ref()
+        .and_then(|m| m.get_param(mime::CHARSET))
+        .map(|c| c.as_str().to_string());
+    Ok(Response {
+        blob: resp.body,
+        mimetype,
+        encoding,
+    })
 }
 
 /// Retrieves the binary contents of URL using HTTP GET request.
 pub(crate) async fn read_url_blob_with_tls(
-    _context: &Context,
-    _url: &str,
+    context: &Context,
+    url: &str,
     _strict_tls: bool,
 ) -> Result<Response> {
-    bail!("HTTP requests are not available on wasm yet")
+    read_url_blob(context, url).await
 }
 
 /// Sends an empty POST request to the URL.
-pub(crate) async fn post_empty(_context: &Context, _url: &str) -> Result<(String, bool)> {
-    bail!("HTTP requests are not available on wasm yet")
+///
+/// Returns response text and whether request was successful.
+pub(crate) async fn post_empty(_context: &Context, url: &str) -> Result<(String, bool)> {
+    ensure_https(url)?;
+    let resp = fetch("POST", url, None, None).await?;
+    Ok((String::from_utf8_lossy(&resp.body).to_string(), resp.ok))
 }
 
 /// Sends a POST request with given string as body.
-pub(crate) async fn post_string(_context: &Context, _url: &str, _body: String) -> Result<bool> {
-    bail!("HTTP requests are not available on wasm yet")
+pub(crate) async fn post_string(_context: &Context, url: &str, body: String) -> Result<bool> {
+    ensure_https(url)?;
+    Ok(fetch("POST", url, Some(body), None).await?.ok)
 }
 
 /// Posts the given form to the URL.
 pub(crate) async fn post_form<T: Serialize + ?Sized>(
     _context: &Context,
-    _url: &str,
-    _form: &T,
+    url: &str,
+    form: &T,
 ) -> Result<bytes::Bytes> {
-    bail!("HTTP requests are not available on wasm yet")
+    ensure_https(url)?;
+    let body = serde_urlencoded::to_string(form).context("Failed to encode data")?;
+    let resp = fetch(
+        "POST",
+        url,
+        Some(body),
+        Some("application/x-www-form-urlencoded"),
+    )
+    .await?;
+    Ok(resp.body.into())
 }
